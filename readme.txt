Need to flow some steps for run this project.

1) create new file from database_simple.php along with to database.php and save it same location. File location (swimmingcoach/application/config/database.php).

2) find these content and changes correct value.

'hostname' => 'host name of database',
'username' => 'username of database',
'password' => 'password of database',
'database' => 'database name',

3) create new file from .htaccess_simple file along with to .htaccess and save it same location. File location (luckydraw/)
